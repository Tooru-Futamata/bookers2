     <% if @user.errors.any? %>
     <h2><%= @book.errors.count %>error prohibited this obj from being saved:</h2>
     <table><% @book.errors.full_messages.each do |message| %></table>
     <%= message %>
     <% end %>
     <% end %>
<%= render 'books/list', book: @book,user: current_user %>
    <div class='col-md-8 offset-md-1'>
      <h2>Users</h2>
      <table class='table table-hover table-inverse'>

  <thead>
    <tr>
      <th>image</th>
      <th>name</th>
      <th></th>
      <th></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
  <% @users.each do |user| %>
  <tr>
  <td>
    <%= attachment_image_tag user, :profile_image, :fill, 40, 40, fallback: "no_image.jpg", class:"mb-3" %>
  </td>
  <td><%= user.name %></td>
  <td><%= link_to "Show", user_path(user.id) %></td>
  </tr>
  <% end %>
  </tbody>
  </table>
  </div>
  </div>
  </div>